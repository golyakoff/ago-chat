<Project Sdk="Microsoft.NET.Sdk">

  <!-- `13-02`: a project of its own because ЮKassa is a distinct external technology -
       `naming-and-structure.md`'s "one project per external technology" rule, the same reason
       Ago.Chat.Infrastructure.MaxBot/Telegram/Keycloak exist. Everything that knows ЮKassa's own HTTP
       shape (its DTOs, its Basic-auth credential pair, its webhook signature scheme) lives here and
       nowhere above it. -->
  <ItemGroup>
    <ProjectReference Include="..\Ago.Chat.Application\Ago.Chat.Application.csproj" />
  </ItemGroup>

  <ItemGroup>
    <PackageReference Include="Microsoft.Extensions.Options" />
    <PackageReference Include="Microsoft.Extensions.Http" />
  </ItemGroup>

</Project>
