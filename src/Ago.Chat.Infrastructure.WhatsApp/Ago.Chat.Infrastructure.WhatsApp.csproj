<Project Sdk="Microsoft.NET.Sdk">

  <!-- `14-10`: a project of its own because WhatsApp is a distinct external technology -
       `naming-and-structure.md`'s "one project per external technology" rule, the same reason
       Ago.Chat.Infrastructure.MaxBot/.Telegram/.Vk each exist. Everything that knows Meta's own Graph
       API shape (its DTOs, its base URL, its webhook verification/signature scheme) lives here and
       nowhere above it - ChannelPortTests.NoProviderVocabulary_AppearsAboveInfrastructure already lists
       "WhatsApp" among the forbidden name fragments above this boundary (it was added ahead of this
       item, alongside "Twilio"/"Viber"/"Smpp", when `14-01` first wrote that test). -->
  <ItemGroup>
    <ProjectReference Include="..\Ago.Chat.Application\Ago.Chat.Application.csproj" />
  </ItemGroup>

  <ItemGroup>
    <PackageReference Include="Microsoft.Extensions.Options" />
    <PackageReference Include="Microsoft.Extensions.Http" />
    <PackageReference Include="Microsoft.Extensions.Logging.Abstractions" />
    <PackageReference Include="Microsoft.Extensions.DependencyInjection.Abstractions" />
  </ItemGroup>

</Project>
